[project]
name = "example-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "example-lib",
]

[tool.uv.sources]
example-lib = { path = "../example-lib", editable = true }
